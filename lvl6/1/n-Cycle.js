function cycle(n) {
    let a = 1 / n;
    let b = a.toString().split('');
    let c = b.length;
    console.log(b, c);
}

cycle(33);



for (let i = 0; i < b.length; i++) {
    if (b[i] == b[i + 1]) {
        return true;
    } else {
        return -1;
    }
}